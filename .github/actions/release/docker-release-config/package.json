{
  "description": "dependencies for releasing the docker image through semantic release",
  "devDependencies": {
    "@semantic-release-plus/docker": "^3.1.2",
    "conventional-changelog-eslint": "^4.0.0",
    "semantic-release": "^21.0.5"
  }
}
