name: Base debugging workflow

on: [push]
    #secrets: inherit
    #permissions: {}

jobs:
  # gen-app-artifact:
  #   name: workflow
  #   uses: ./.github/workflows/gen-app-artifact.yml

  # gen-docker-artifact:
  #   name: workflow
  #   needs: gen-app-artifact
  #   uses: ./.github/workflows/gen-docker-artifact.yml

  # gen-wiki-artifact:
  #   name: workflow
  #   uses: ./.github/workflows/gen-wiki-artifact.yml

 
  gen-wiki-artifact:
    name: workflow
    uses: ./.github/workflows/gen-artifacts.yml
    with:
      artifacts: '["docker", "app"]'
